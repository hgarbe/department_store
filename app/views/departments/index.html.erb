<h1>Check Out Our Shoes!</h1>
<%= link_to 'Create a new department', new_department_path %>
<ul>
  <% @departments.each do |department| %>
    <li><%= link_to department.name, department_path(department) %></li>
  <% end %>
</ul>